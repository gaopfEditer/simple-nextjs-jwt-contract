# 数据库配置
DB_HOST=localhost
DB_PORT=3306
DB_USER=nextjs_user
DB_PASSWORD=nextjs_password
DB_NAME=nextjs_jwt

# JWT 配置
JWT_SECRET=your-secret-key-change-in-production-min-32-characters
JWT_EXPIRES_IN=7d

# Next.js 配置
NODE_ENV=production
PORT=3000

# WebSocket IP 配置
# 本地开发：不设置这些变量，将自动使用本机局域网 IP
# 服务器部署：在 .env.local 中设置以下变量之一
# PUBLIC_DOMAIN=your-domain.com         # 使用域名（优先级最高，推荐用于生产环境）
# PUBLIC_IP=your-server-ip               # 直接指定公网 IP 地址（如果使用 IP 而非域名）

# (可选) 其他数据库连接
# REDIS_URL=redis://default:foobared@localhost:6379
# MONGODB_URI=mongodb://admin:password@localhost:27017/dbname?authSource=admin
# PG_URL=postgresql://postgres:password@localhost:5432/postgres

